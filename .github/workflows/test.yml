name: Test

on:
  push:
    branches:
      - main

jobs:
  test-macos:
    runs-on: macos-latest
    steps:
      - uses: irgaly/setup-mint@main
        with:
          mint-directory: example
      - run: |
          ls -alh /usr/local/bin | grep mint
          mint --version
          mint list
          ls -alh ~/.mint
          ls -alh ~/.mint/packages/*
  test-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: irgaly/setup-mint@main
        with:
          mint-directory: example
      - run: |
          ls -alh /usr/local/bin | grep mint
          mint --version
          mint list
          ls -alh ~/.mint
          ls -alh ~/.mint/packages/*
